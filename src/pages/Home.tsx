import React, { FC } from 'react';
import { Typography } from "@mui/material";

const Home: FC = () => {
  return (
    <Typography variant="h1">
      Mealtime
    </Typography>
  );
};

export default Home;
