import { createTheme } from '@mui/material/styles';

export const mealtimeTheme = createTheme({
  typography: {
    h1: {
      fontSize: "3rem",
    },
    h2: {
      fontSize: "2rem",
    },
  },
});
